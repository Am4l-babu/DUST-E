# PlatformIO build output. Regenerated by `pio run`; several MB per project.
.pio/
.pioenvs/
.piolibdeps/

# Arduino IDE build cache
build/
*.elf
*.map

# Brain (Python, UNO Q). Detector models are exported per brain/README.md,
# never committed: large binaries with their own licences.
__pycache__/
.pytest_cache/
*.pyc
brain/models/*.onnx
brain/models/*.tflite
*.jsonl

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
